extends ../layout

block content
  h1= title
  p 
    | Are you sure you want to delete 
    em= book.title
    | ?
  form(action="/users/" + book.id + "?_method=DELETE", method="post")
    a(href="/users/" + book.id) No! Take me back!
    | 
    input(type="submit", value = "Yes, I am")