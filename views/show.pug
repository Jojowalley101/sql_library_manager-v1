extends ../layout

block content
  h1= book.title
  include by_line
  p= book.body
  a(href="../") &larr; Back
  | | 
  a(href="/users/" + book.id + "/edit") Edit Book
  | | 
  a(href="/users/" + book.id + "/delete") Delete Book